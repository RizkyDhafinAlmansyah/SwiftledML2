services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: swiftlet
      POSTGRES_USER: swift
      POSTGRES_PASSWORD: swiftpass
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: {}
